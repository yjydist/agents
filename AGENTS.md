# 仓库维护约定

本文件面向维护本仓库的 agent, 约束本仓库内容的创建, 修改和验证. 供其他项目复制的工程原则存放在 [template/agent.md](template/agent.md).

## 仓库定位与内容职责

本仓库存放可按需复制和组合使用的工程模板, 技能和专用 subagent 提示词, 不提供下载, 安装或同步机制. 内容可以依赖其他技能, 模板, 角色或外部工具, 在 README 中说明必需依赖, 条件依赖和适用范围.

- `template/agent.md` 保留值得跨任务提醒的核心工程判断, 具体工作方法归入技能.
- `skills/<技能名>/SKILL.md` 提供按任务加载的方法, 可以组合已有能力. 正文在需要依赖的位置说明如何使用, README 维护对应依赖说明.
- `agents/<角色名>.md` 定义专用角色的工作边界, 最小方法与交付结果. 这些文件是提示词来源, 实际注册和权限由目标工具负责. 保持角色只调查, 返回证据且不继续委派的边界, 不复制完整技能流程.
- `README.md` 面向使用者说明用途, 使用方式并维护技能与角色索引. 本仓库维护约定集中在本文件.

## 内容演进

- 保留敏捷开发主线: 小步交付可验证的完整行为, 用真实反馈调整需求理解与实现, 通过持续重构保持低成本变更能力. 模板, 技能与角色的调整都应支持这一循环.
- 根据真实任务中的失败, 验证结果和维护成本改进内容. 新增规则前确认它解决什么问题, 何时适用, 是否与已有内容重复或冲突; 不为假想需求扩充流程.
- 优先合并, 改写和删除, 根据理解与维护成本选择复用或少量重叠. 模型通常已具备的一般知识可直接删去, 不要求每条旧规则另找存放位置.
- 阅读并提炼参考材料, 将适用的判断方法直接写入正文. 不堆叠原则名称或特定项目流程, 不把阅读原资料作为使用技能的前提.
- 文档正文使用简体中文和 ASCII 标点, skill 的 `description` 使用英文, 技术名称与链接保留原文. 新增, 删除, 重命名或改变技能与角色职责时, 同步更新 README 对应索引和受影响的引用.

## Skill 的阶段与结构

- 需要充分调查, 比较或判断的阶段, 如果后续实施目标容易使其被草率跳过, 将其作为独立技能, 明确当前产出, 证据要求和结束条件. 不在该技能中塞入后续阶段的完整流程, 不按每个操作步骤机械拆分.
- 阶段输入可以来自当前任务, 已有证据或依赖技能的产出. 已有充分依据时直接进入需要的阶段, 不为形式补跑前置阶段或生成额外文档; 依据失效时调查具体疑点, 不重跑整套流程.
- 阶段完成不等于用户任务完成. 已授权的实施任务继续推进至完整交付, 不因技能拆分新增审批或要求用户逐阶段点名. 仅请求调查或方案时遵守该范围.
- 常用步骤与必要判断留在正文, 只在某个分支需要的较长模板或参考移到技能目录内, 由条件明确的链接按需加载. 不为缩短主文件而外移每次都需要的材料.
- 对重复或疑似无效的规则, 用它要改变的具体行为判断保留价值, 合并同义表达. 精简时保留仍有效的约束; 引导词是否出现只是线索, 以实际选择, 行动和产出验证效果.

## Skill 的触发描述

- 新建或修改技能时, 将何时使用写入 YAML frontmatter 的 `description`. 模型应仅凭名称与描述就能判断是否适用, 不依赖正文, README 或其他技能补充触发条件.
- 统一使用 `description: |-` 多行英文字符串, 内容以两个空格缩进. 先用一句话概述能力与结果, 再用 `Use when:` 和条目列出使用条件; 仅在容易误判时增加 `Not for:` 段落. 能力概述与触发时机必选, 不适用整段可省略. 描述用于选择技能, 不复制正文流程, 将常驻上下文控制在识别适用边界所需的范围内.
- 触发条目描述用户意图和当前任务中可识别的问题信号, 同时覆盖明确请求与工作中出现实际需要的场景. 任一条成立即可考虑使用, 不要求全部满足, 不要求用户说出技能名或固定关键词; 同时核对是否属于不适用场景, 避免正反条件冲突.
- 支持按实际需要主动调用. 常规开发中的补测试, 交付自检或编码完成, 不自动要求加载整套技能; 描述中的主动调用应对应具体问题, 而非只要接触某个主题就触发.
- 固定结构, 保持表达简洁且有区分度. 不用空泛价值宣言, 关键词堆砌或完整操作流程替代适用条件, 不固定条目数, 句式或长度, 不为填满模板编造场景或排除条件. 操作流程和权限细节保留在正文.
- 检查相邻技能的区别与合理组合. 例如, 未知测试失败先定位原因, 确认存在测试维护问题后再调整测试; 解释当前实现不必查历史, 决策依赖历史原因时再调查. 依赖调用应对应实际需要, 不为普通任务强制加载整套技能.
- 描述必须与正文提供的能力和工作边界一致. 主动调用不扩大当前任务, 不把诊断变成擅自修复, 不把提交建议变成创建提交或改写历史的授权. 调整触发策略时同步修正正文中的实际冲突.
- 保持技能目录名与 frontmatter `name` 一致, 保留仍有效的 metadata. 详细流程, 方法和交付要求放在正文; 不为优化描述添加无实际需要的文件或框架.

以故障排查为例, 下面的结构可复用, 具体条件须按技能的真实能力编写:

```yaml
description: |-
  Investigate failures and establish the root cause with reproducible evidence.
  Use when:
  - A user reports a bug whose cause is still unknown.
  - An unexplained error or failed check blocks the current task.
  Not for:
  - Implementing an already established correction or reorganizing tests.
```

## 验证与交付

- 修改前阅读相关技能正文, 相邻技能描述和引用, 理解真实能力与已有边界, 检查工作区已有修改.
- 校验受影响技能的 YAML frontmatter, 确认 `name` 和 `description` 为有效非空字符串, 多行描述的换行被保留, 名称与目录一致, 未意外丢失 metadata.
- 仅看名称与 description, 用明确请求, 执行中发现实际需要, 相邻技能易混淆场景和普通步骤核对选择依据. 确认适用场景能被识别, 易误触场景有足够区分, 再与正文核对一致性. 不用固定措辞匹配代替语义检查.
- 调整阶段边界时, 检查原有能力和关键约束由何处承接, 分别核对仅请求分析, 已授权实施, 已有阶段输入和证据不足的场景. 不把阶段交付误写成整个任务停止条件.
- 检查受影响的 Markdown 链接, README 索引与依赖说明, 确认必需依赖和条件依赖与正文一致. 审阅实际差异并运行 `git diff --check`. 新增或修改可执行资源时, 运行与其实际行为相关的检查.
- 交付时说明结果, 实际验证和剩余限制. 格式校验与静态场景核对不能证明真实模型的触发率提升; 触发效果依据真实任务反馈改进, 未运行的验证不得声称通过.
